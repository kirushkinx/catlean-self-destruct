name: build

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25
          cache: gradle

      - name: Build
        run: |
          chmod +x gradlew
          ./gradlew build --no-daemon

      - name: Artifact name
        id: meta
        run: echo "name=catlean-self-destruct-$(git rev-parse --short HEAD)+${GITHUB_REF_NAME//\//-}" >> "$GITHUB_OUTPUT"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.meta.outputs.name }} # catlean-self-destruct-{gitHash}+{branch}
          path: |
            build/libs/*.jar
            !build/libs/*-sources.jar
